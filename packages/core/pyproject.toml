[project]
name = "ariadion-core"
version = "0.1.0rc1"
description = "Shared identity and source-location contracts for Ariadion"
requires-python = ">=3.11"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]
