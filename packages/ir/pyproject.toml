[project]
name = "ariadion-ir"
version = "0.1.0"
description = "Semantic intermediate representation for Ariadion"
requires-python = ">=3.11"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]
