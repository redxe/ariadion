# Ariadion Studio

This directory reserves the desktop IDE product boundary.

The first Studio vertical slice should present four synchronized panes:

1. Ariadion source;
2. semantic circuit rendered from IR;
3. Theonoe state inspection;
4. diagnostics and resource estimates.

Studio should call the runtime through a versioned protocol. It should not import compiler internals directly.
